&НаКлиенте
Процедура ЗаписатьНастройки(Команда)
    Попытка
        // Просто обновляем список - изменения сохранятся автоматически
        Элементы.Список.Обновить();
        Сообщить("Настройки сохранены");
    Исключение
        Сообщить("Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьНастройки(Команда)
    Попытка
        Элементы.Список.Обновить();
        Сообщить("Данные обновлены");
    Исключение
        Сообщить("Ошибка: " + ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
    // Ничего не делаем
КонецПроцедуры